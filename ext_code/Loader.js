// Loader Component
// Simpler loader shown when processing

import React from "react";
import { ActivityIndicator } from "react-native";

const Loader = () => {
  return <ActivityIndicator size="large" color="#9013FE" />;
};

export default Loader;